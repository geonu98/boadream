import {
  getCurrentAdminProfile,
  getCurrentSession,
  signInAdmin,
  signOutAdmin,
} from "./authRepository";

export const authService = {
  getCurrentAdminProfile,
  getCurrentSession,
  signInAdmin,
  signOutAdmin,
};
